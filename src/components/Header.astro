---
import { getCVInformation } from "../utils/getCVInformation"

const items = Object.keys(getCVInformation()) as (keyof ReturnType<typeof getCVInformation>)[] 
---
<header>
	<h1>Zhifan</h1>
	<menu>
		{items.map(item => <li><a>{item}</a></li>)}
	</menu>
	<!-- Placeholder -->
	<div>

	</div>
</header>

<style>
	header {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}

	menu {
		display: flex;
	}

	li {
		list-style: none;
		padding: 1rem;
	}
</style>

---
import { getCVInformation } from '../utils/getCVInformation'

const cvInformation = getCVInformation()
---

<section>
    <h2>Resume of education and work experiences</h2>
    <ol>
        {
            cvInformation.Experiences.map((workExperience) => {
                return (
                    <li>
                        <h3>{workExperience.company}</h3>
                        <p>{workExperience.period.toString()}</p>
                        <h4>Projects</h4>

                        <a
                            href="#"
                            class="block max-w-sm p-6 bg-white border border-gray-200 rounded-lg shadow hover:bg-gray-100 dark:bg-gray-800 dark:border-gray-700 dark:hover:bg-gray-700"
                        >
                            <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">
                                Noteworthy technology acquisitions 2021
                            </h5>
                            <p class="font-normal text-gray-700 dark:text-gray-400">
                                Here are the biggest enterprise technology
                                acquisitions of 2021 so far, in reverse
                                chronological order.
                            </p>
                        </a>

                        <ol>
                            {workExperience.projects
                                .map((project) => {
                                    return (
                                        <li>
                                            <p>{project.name}</p>
                                            <p>{project.description}</p>
                                            <p>{project.period.toString()}</p>
                                        </li>
                                    )
                                })
                                .reverse()}
                            {workExperience.projects
                                .map((project) => {
                                    return (
                                        <li>
                                            <p>{project.name}</p>
                                            <p>{project.description}</p>
                                            <p>{project.period.toString()}</p>
                                        </li>
                                    )
                                })
                                .reverse()}
                        </ol>
                    </li>
                )
            }).reverse()
        }
    </ol>
</section>
